# My site

## Prerequisites

- [Install Ruby & Jekyll](https://jekyllrb.com/docs/#instructions)

## Build steps

1. Run: `bundle` to install dependencies.
2. Run: `bundle exec jekyll serve` to serve static HTML content in development mode
3. Run `bundle exec jekyll build` to build static HTMl content

Phat Nguyen
